<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daftarmahasiswa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Semua activity aplikasi -->
        <activity android:name=".item" />
        <activity android:name=".tambah" android:parentActivityName=".home" />
        <activity android:name=".edit" />
        <activity android:name=".profile" />
        <activity android:name=".home" />
        <activity android:name=".Login" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".setting" /> <!-- ✅ Ini yang ditambahkan -->

        <!-- Activity utama (splash screen) -->
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>
